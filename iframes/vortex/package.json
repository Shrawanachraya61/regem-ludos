{
  "name": "arcade_cpp_vortex",
  "version": "1.0",
  "license": "MIT",
  "scripts": {
    "start": "(cd .. && http-server  --mimetypes ../scripts/mime.types -p 9029 -o /vortex/dist/?tap=true)",
    "build": "yarn clean && (cd src && make js) && yarn mv",
    "build:dev": "yarn build",
    "mv": "mkdir -p dist && cp -t dist/ public/*",
    "clean": "rm -rf dist"
  },
  "dependencies": {}
}
