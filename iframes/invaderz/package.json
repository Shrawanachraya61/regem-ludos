{
  "name": "arcade_cpp_invaderz",
  "version": "1.0",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && (cd src && make js) && yarn mv",
    "mv": "mkdir -p dist && cp -t dist/ public/* -v",
    "clean": "rm -rf dist"
  },
  "dependencies": {}
}
