{
  "name": "arcade_cpp_elasticity",
  "version": "1.0",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && (cd src && make js) && yarn mv",
    "build:dev": "yarn build && yarn mv:dev",
    "mv": "mkdir -p dist && cp -t dist/ public/* -v",
    "mv:dev": "node debug.script.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {}
}
