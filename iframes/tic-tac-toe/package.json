{
  "name": "arcade_tictactoe",
  "version": "1.0",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && ../../node_modules/.bin/terser tic-tac-toe.js --keep-fnames -o dist/tic-tac-toe.js && yarn mv",
    "build:dev": "yarn build",
    "mv": "mkdir -p dist && cp -v tic-tac-toe.html favicon.png Retron2000.ttf *.mp3 dist/ ",
    "clean": "rm -rf dist"
  },
  "dependencies": {}
}
