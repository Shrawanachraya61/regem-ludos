{
  "name": "arcade_tictactoe",
  "version": "1.0",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && ../../node_modules/.bin/terser tic-tac-toe.js --keep-fnames -o dist/tic-tac-toe.js && yarn mv",
    "mv": "mkdir -p dist && cp -t dist/ tic-tac-toe.html favicon.png Retron200.ttf -v",
    "clean": "rm -rf dist"
  },
  "dependencies": {}
}
